<div class="planet-packages">
  <% @destination.packages.each do |package| %>
    <%= link_to package.title, package_path(package) %>
    <%= link_to(image_tag(package.image, alt: package.image), package_path(package))%>
  <% end %>
</div>
